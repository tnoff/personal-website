name: CD

on:
  pull_request:
    types:
    - closed

jobs:
  tag_build:
    permissions:
      contents: write
    uses: tnoff/github-workflows/.github/workflows/tag.yml@06f676738efdf3f327af0059b3ce0c6f187e1b5c
    with:
      version_file: ./VERSION
  check_labels:
    permissions:
      contents: read
      pull-requests: read
    uses: tnoff/github-workflows/.github/workflows/check-pr-labels.yml@5c01c5cfcd5237914aee049f8b681b1a0b6215cc
    with:
      required_labels: 'build-docker'
      require_all_labels: true
      require_merged: true
  build:
    permissions:
      contents: read
    needs: check_labels
    if: needs.check_labels.outputs.conditions_met == 'true'
    uses: tnoff/github-workflows/.github/workflows/ocir-push.yml@5c01c5cfcd5237914aee049f8b681b1a0b6215cc
    with:
      image_name: ${{ vars.OCI_REPO_NAME }}
      platforms: linux/amd64,linux/arm64
    secrets:
      oci_registry: ${{ secrets.OCI_REGISTRY }}
      oci_username: ${{ secrets.OCI_USERNAME }}
      oci_token: ${{ secrets.OCI_TOKEN }}
      oci_namespace: ${{ secrets.OCI_NAMESPACE }}
